language: node_js
node_js:
  - "stable"
notifications:
  email: false

cache:
  directories:
  - node_modules

script:
  npm run build && npm run lint && npm test

deploy:  
  github-token: $travis
  on:
    branch: master